{
  "role": {
    "name": "json.roles::schema_access",
    "schema_privileges": [
      {
        "privileges": [
          "SELECT",
          "SELECT METADATA",
          "EXECUTE"
        ]
      }
    ]
  }
}