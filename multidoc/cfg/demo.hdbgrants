{
  "JSONDEMO": {
    "object_owner": {
      
      "system_privileges": [{"privileges": ["CATALOG READ"]}],
      "schema_privileges" : [
        {
          "schema" : "BTP",   // optional; override schema defined in grantor service
          "privileges" : [ "INSERT", "UPDATE", "SELECT", "EXECUTE", "CREATE ANY" ],
          "privileges_with_grant_option" : [ "INSERT", "UPDATE", "SELECT", "EXECUTE", "CREATE ANY" ]
        }
      ]
      
      
    },
    "application_user": {
      "system_privileges": [{"privileges": ["CATALOG READ"]}],
      "schema_privileges": [
        {
            "schema" : "BTP",   // optional; override schema defined in grantor service
            "privileges" : [ "INSERT", "UPDATE", "SELECT", "EXECUTE", "CREATE ANY" ]            
        }
      ]
      
    }
  }
}